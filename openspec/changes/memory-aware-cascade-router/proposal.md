# Proposal: memory-aware cascade router

Replace task-name-only dispatch with a policy that considers capability, memory state,
quality, risk, and candidate readiness. Keep the policy deterministic and explainable
until sufficient routing labels exist for a learned router.
